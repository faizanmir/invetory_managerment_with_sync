<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    >
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:textSize="20sp"
        tools:text="INCREASE COUNT BY"
        android:textColor="@color/black"
        android:id="@+id/title_inc_dsc"

        />
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/title_inc_dsc"
        android:background="@color/white"
        tools:text="Count"
        android:hint="New Count"
        android:textSize="20sp"
        android:padding="15dp"
        android:layout_marginVertical="10dp"
        android:id="@+id/new_count"
        android:inputType="number"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/new_count"
        android:text="Change count"
        android:textAllCaps="false"
        android:textSize="15sp"
        android:padding="10dp"
        android:layout_marginVertical="10dp"
        android:id="@+id/change_count_button"
        />



</androidx.constraintlayout.widget.ConstraintLayout>
</layout>